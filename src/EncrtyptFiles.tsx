/* eslint-disable no-mixed-spaces-and-tabs */
export class EncryptFiles {

    // GetTotalBytesAndFileInfo = ((file: any, totalBytes: any, fileInfoCollection: any) => {
    // 	return new Promise((resolve) => {
    // 		const fileType = file.type;
    // 		const fileName = file.name;   
    // 		const reader = new FileReader();
			
    // 		reader.onload = (async (event: any) => {	
    // 			const byteArrayForThisFile = this.ByteArrayToArray(event.target.result);
	// 			totalBytes =	this.ArrayConcat(totalBytes, byteArrayForThisFile);
    // 		});
    // 		reader.readAsArrayBuffer(file); 
    // 		resolve();
    // 	});
    // 	//return {TotalBytes, FileInfoCollection}
    // });

    // GetCompressedBytes = ((TotalBytes: any) => {
    // 	//return CompressedBytes
    // });

    // GetMessageUnits = ((CompressedBytes: any, FileInfoCollection: any) => {
    // 	//return MessageUnits
    // });

    // CreateOutputFileas = ((MessageUnits: any) => {});

}
